<% Attraction.all.each do |attraction| %>
    <%= attraction.name %>
    <%= link_to "#{@message} #{attraction.name}", attraction_path(attraction) %>
    <% end %>
<%= link_to "New Attraction", new_attraction_path %>